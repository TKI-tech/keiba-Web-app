# ローカルで動作確認済みの組合せに完全固定している。範囲指定(>=)だと、
# デプロイ先でのビルド時に手元より新しいバージョンが解決され、依存関係の破壊的変更
# (例: starletteのGZipResponderにthread_minimum_sizeが必須引数として追加された等)
# を突然踏むことがあるため。更新する際は、ローカルで一通り動作確認してから
# `pip freeze` の結果で置き換えること。

streamlit==1.60.0
pandas==3.0.5
stripe==15.4.0
python-dotenv==1.2.2
altair==6.2.2

# streamlitが内部で使う主要な依存(バージョンドリフト防止のため明示的に固定)
starlette==1.3.1
uvicorn==0.52.1
websockets==16.1.1
httptools==0.8.0
anyio==4.14.2
h11==0.16.0
python-multipart==0.0.32
pyarrow==24.0.0
pydeck==0.9.3
numpy==2.5.1
pillow==12.3.0
protobuf==7.35.1
tenacity==9.1.4
toml==0.10.2
watchdog==6.0.0
GitPython==3.1.57
blinker==1.9.0
click==8.4.2
Jinja2==3.1.6
